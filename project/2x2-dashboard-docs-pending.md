---
name: Dashboard docs update pending
description: 2x2 dashboard documentation needs updating after trigger page rework and AC quality table changes (May 2026)
type: project
---

Документация по дашборду 2x2 устарела — не отражает последние изменения.

**Что изменилось без обновления доков:**
- Страница `/trigger` переработана: карточки тиражей с 12 триггерами (гориз. + диагональные), детали по клику (коммит `83e7788`)
- Тепловая карта заменена на таблицу «Качество анти-кандидатов» (коммит `550a3f3`)
- Таблица AC quality: per-analyzer бейджи (TB, Tr, DT, Mo) с hit/miss цветами — показывает какой анализатор даёт качественные AC
- Новые API: `/api/trigger_full_analysis`, `/api/ac_quality_history` (ответ теперь включает `analyzer_details`)
- **2026-06-01:** Реорганизация страниц — AC quality и статистика AC перемещены со страницы `/trigger` на `/beams`. Страница `/beams` полностью переписана в стиле триггерной (CSS-переменные, .card layout, sticky nav). На `/trigger` остались только триггерные данные. На `/beams`: Настройки параметров сетов + Статистика качества AC (50/100/500) + Прогноз + История.

**Why:** Пользователь хотел поручить обновление документации, но сессия упала. В сессии 2026-05-26 подтвердил задачу — попросил пояснения по цветам таблицы, уточнил про полноту охвата анализаторов (таблица уже включает все анализаторы, не только триггеры). Готов к обновлению доков.

**How to apply:** Обновить документацию дашборда 2x2 на сервере. Проверить какой файл документации существует (`/root/projects/2x2/`), привести в соответствие со страницей `/trigger`. Включить: структуру карточек тиражей (гориз. + диагональные триггеры), таблицу AC quality с расшифровкой цветов, новые API endpoints.
