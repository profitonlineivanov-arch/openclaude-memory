[Model List Reliability Protocol] Tool output (e.g., `ollama list`) must be the authoritative source of truth for currently installed local models. **Why:** User observed discrepancy between perceived model presence and tool output. Relied on an unlisted model (`gemma4:12b-mlx`), causing unnecessary removal attempts that failed due to non-existence in the official list. **How to apply:** If `ollama list` does not show a model, assume it is absent or unavailable via CLI; do not attempt remediation/removal until confirmed by working tool output.